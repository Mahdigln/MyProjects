namespace PrimaryConstructors;

//it is like record but this is constructor
//notice that name is not read only so you can change it
public class Person(string _name)
{
   
}
record User(string Name);